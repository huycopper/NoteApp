<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange</item>  <!-- Màu chủ đạo (ví dụ: Button, Toolbar) -->
        <item name="colorPrimaryVariant">@color/orange</item> <!-- Biến thể màu chủ đạo -->
        <item name="android:statusBarColor">@color/orange</item> <!-- Màu của dải pin/giờ trên cùng -->
    </style>

    <style name="Theme.NoteApp" parent="Base.Theme.NoteApp" />
</resources>